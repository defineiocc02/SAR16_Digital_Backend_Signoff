# 修复 07c：GDS 放置**完全对上**——两处"异常"确认都是我的解析器错

**日期**：2026-09-19 ｜ **状态**：✅ **GDS 项关闭** ｜ 承接：修复 07（记录码表错）、修复 07b（SREF 口径错）

---

## 1. 顶层 cell 实际引用了什么（干净的一次测量）

```
structs defined in the file : 225
TOP cell                    : sar_digi_paper_core
distinct masters referenced : 115
total SREFs in TOP          : 34356

master referenced BY THE TOP          count
$$via1                                12931
$$via2                                12613
$$via3                                 3584
$$via1_5200_5200_1_2                   1462
DFFSXL                                  781
NAND2XL                                 528
NOR2XL                                  236
DFFSX1                                  215
INVXL                                   206
OAI21XL                                 187
…（其余为库单元，略）
$$via4                                  134
```

## 2. 三个数字**逐项对上**，一分不差

| 量 | 实测 | 应当等于 | 对否 |
|---|---|---|---|
| distinct masters | **115** | 6 个 `$$via*` + **109** 个库单元（LEF 的 MACRO 数） | ✅ |
| total SREFs | **34 356** | 30 730 个过孔阵列 + **3 626** 个单元（网表的单元数） | ✅ |
| structs | **225** | 109 包装 + 109 内部哈希名 + 顶 cell + 6 `$$via` = **225** | ✅ |

（过孔：via1 12 931 + via2 12 613 + via3 3 584 + via1_5200… 1 462 + via4 134 + via5 6 = **30 730**）

**⇒ 交付 GDS 的放置与网表、LEF 三方完全自洽。**

## 3. 两处"异常"的结论：**都是我的错**

| 第 22 轮我报的异常 | 真因 | 处置 |
|---|---|---|
| ①「顶层 cell 没有任何几何」 | 我的 GDS 记录码表写错（`0x08` 才是 BOUNDARY，我写成 ENDEL） | **撤回**（修复 07） |
| ②「顶层放置 3735 vs 网表 3626，多 109 / master 224 种」 | 我把**全文件**的 SNAME 计数当成了**顶层**放置 | **撤回**（修复 07b/07c） |

**⇒ 被核查件在这两点上没有问题。核查报告里"GDS 两处异常"整节应改为"我方解析器错误，已更正"。**

## 4. GDS/布线这条现在**站得住的全部结论**

| 结论 | 值 | 来源 |
|---|---|---|
| 单元放置数 | **3 626**，109 种 | 本次 GDS 实测 = 网表 = LEF |
| 过孔阵列 | **30 730** 个放置，6 种 | 本次 GDS 实测 |
| 顶层金属（P&R 自己画的） | **37 836 个多边形 / 103 969 µm²** | 本次 GDS 实测 |
| 分层用量 | M1 7 911 / M2 17 445 / M3 10 115 / M4 2 290 / **M5 72 / M6 2** | 本次 GDS 实测 |
| 利用率 | **0.5558** | `fc_util.rpt` |
| 布线溢出 | **2.52 %** GRC | `fc_congestion.rpt` |
| 短路口径 | **0** | `lvs.rep.shorts` 不存在 |
| DRC | 421 规则 / **3 402** 结果 | 独立重解析 `drc_CAL.SUM` |

**一句话：GDS 是自洽的；布线"散"的原因是核心面积给了 `set UTIL 0.55`，且 M5/M6 几乎没被用到。**

## 5. 边界

- 面积按**多边形包围盒**累加、非真实金属覆盖（重叠会高估），**只能用于层间相对比较**；
- 只统计**顶层**几何，不含单元内部；
- 原始输出：`evidence/v51/gds_top_refs.txt`、`gds_routing_census.txt`。

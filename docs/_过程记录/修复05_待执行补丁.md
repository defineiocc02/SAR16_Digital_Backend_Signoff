# 修复日志 05：**待执行的修复补丁**（未应用、未验证）

**日期**：2026-09-19 ｜ **状态**：**补丁已写清，但一行都没执行** —— 本轮预算不足以跑完 P&R 并核验
**承接**：修复 01–04（复位丢失发生在 P&R；库里有 TIEHI/TIELO 但脚本未绑定；存在 1 个非默认 tie 选项）

---

## 1. 病因（三层，逐层有证据）

| 层 | 事实 | 证据 |
|---|---|---|
| A | RTL 与 **DC 网表**都正确：`.rst_n(rst_n)`，`HFSNET` 0 行 | `rtl/…sv:209/240`；`mapped_ss/…_netlist.v:6114/6122` |
| B | **P&R 脚本没有把 TIEHI/TIELO 绑给 FC** | tcl 无绑定语句；FC 日志 `OPT-200 No tie cell is available` |
| C | 库里 **有** tie cell，NDM 里也**有** | `smic18.cdl` 有 TIEHI/TIELO；FC 查询 `count=1 names=…/TIEHI`、`…/TIELO` |

**⇒ FC 只能用反相器造常量（85 个 `HFSINV_*`），常量根 `HFSNET_120…124` 写不出驱动，
`HFSNET_119 → rst_n` ⇒ 两个最大子模块复位锁死。**

**仍未确定的一环**：`rst_n` 为什么会被判成"可以接常量"。嫌疑项是
`compile.flow.tie_unused_hier_inputs_to_constants`（及其 `_all_` 版本），**未验证**。

---

## 2. 补丁内容（改 `scripts/fc_pnr_paper_core.tcl`）

**改动一：显式绑定 tie 单元**（放在 `link_block` 之后、任何优化之前）

```tcl
# --- 修复：把库里的 tie 单元交给 FC，别让它用反相器造常量 -------------------
# 依据：日志 Warning: No tie cell is available for constant fixing. (OPT-200)
#       而 NDM 里确实存在 sar16_smic18_6lm_v4/TIEHI 与 /TIELO（已用 get_lib_cells 证实）
if {[catch {
    connect_tie_cells \
        -tie_high_lib_cell sar16_smic18_6lm_v4/TIEHI \
        -tie_low_lib_cell  sar16_smic18_6lm_v4/TIELO
} m]} {
    puts "FIX_NOTE connect_tie_cells: $m"
} else {
    puts "FIX_OK tie cells bound: TIEHI / TIELO"
}
```

**改动二：明确禁止"把未用的层次化输入接常量"**（把病因 A 那一环关掉）

```tcl
set_app_options -name compile.flow.tie_unused_hier_inputs_to_constants     -value false
set_app_options -name compile.flow.tie_all_unused_hier_inputs_to_constants -value false
```

> **注意**：改动二的**选项名已用 `get_app_options *tie*` 实证存在**；
> 但**取值语法**本轮没验证（我用错形式，只回显了名字）。
> 若 `set_app_options` 报错，按工具提示改用带 `-name/-value` 的形式。

---

## 3. 执行方式（**必须在副本上，不能动交付产物**）

```bash
# 1) 复制工程（不碰原目录）
cp -a /home/<user>/sar16_work/proj_paper_core /home/<user>/sar16_work/fix_tie
# 2) 只改副本里的脚本
#    应用 §2 的两处改动
# 3) 副本里跑 P&R —— 注意原 stage 脚本会 rm -rf $PRJ/pnr，所以：
#    在副本内执行，或把脚本里的 rm -rf 改成副本自己的输出目录
cd /home/<user>/sar16_work/fix_tie && bash scripts/run_pnr_paper_core.sh
```

**红线**：`/home/<user>/sar16_work/proj_paper_core/pnr/out/` 是现有交付产物，**不得被 `rm -rf`**。

---

## 4. 验证判据（四条，全部可自动核对）

| # | 判据 | 目标 | 当前值 | 怎么核 |
|---|---|---|---|---|
| 1 | 布线后网表 `HFSNET` 行数 | **0** | **1841** | `grep -c HFSNET <pnr>.v` |
| 2 | Formality FM-399「实现侧无驱动网」 | **0** | **5** | LEC 日志里的 `0 (N) undriven nets` |
| 3 | 门级仿真：复位序列后 `busy` | **抬起** | **恒 0** | 自写 TB（复位序列：tie 根 1→0） |
| 4 | LVS | 短路 0、端口 178 | 短路 0、178 | `lvs.rep` |

**判据 1 与 2 是一票否决的**：只要 `HFSNET` 不为 0 或 FM-399 无驱动网不为 0，就没修好。

---

## 5. 本轮的边界（必须写清楚）

- **补丁未应用**：`fc_pnr_paper_core.tcl` 一行未改；
- **未验证**：§4 四条判据一条都没测，**当前值就是修复前值**；
- **改动二的取值语法未验证**（见 §2 注）；
- **没有**把这份补丁说成"已修复"，也没有把它写进交付 README。

# SAR16 后端 · 接手第 4 轮：交付报告（WorkBuddy 风格）+ 工作区整理

**时间**：2026-09-18 20:30–20:40　**性质**：文档产出 + 工作区整理（本轮不再改设计）

---

## 1. 交付物：沿用它模板的 HTML 报告

**`04_工作记录\SAR16_接手报告_缺陷修复与签核_20260918.html`**（24.6 KB）

模板来源：**他那份 `make_delivery_report.py` 里的 `CSS` 常量**（而不是从 1.37 MB 的成品 HTML 里扒）——
`:root` 变量、`h2` 的红青渐变 `linear-gradient(90deg,#FF6B6B,#4ECDC4)`、`.kpi` / `.toc` / `.bdg` /
`.note` / `pre` 深色块 / `.foot`，与他四篇报告的观感一致。

沿用他自己的三条纪律：

1. **每个数字都从原始工件解析，不手抄**（`RESULT_CURRENT.env` / `lvs.rep` / `drc_CAL.SUM` /
   `sta_pc_summary.txt` / `fc_*.rpt`）；
2. 内容先算出来，模板只引用变量；
3. **解析不到写「未取得」，不写 0** —— 0 与未知是两件事。

篇幅结构：`第1篇 缺陷定位与修复` · `第2篇 时序签核` · `第3篇 GDS 与布线` · `第4篇 功能与接口` ·
`附A 工作记录整理与归档` · `附B 交付清单与结论边界`，共 6 个 h2 锚点与目录互链。

**自检**（`tools/chk_html.py`）：20 类标签全配平（delta 全 0）、**零外部引用**、骨架完整 →
`RESULT: PASS`。

### 1.1 报告里如实写下的三处"不好看但必须写"

- **hold 未闭合**：slow 16 条（WNS −0.2020）、fast 12 条、typical 1 条，且端点落在
  **输入端口与门控锁存器**上，属约束建模问题（`calib_comp_out → comp_out_r_reg/D`、
  `raw_bits_i[15] → raw_code_o_reg[15]/D`）。**setup 三角全 0 违例与 hold 违例必须分开写。**
- **本轮改过 RTL 接口 ⇒ 旧的形式化等价/门级仿真结论一律作废**，这是接手带来的**新增验证债**。
- **LVS 仍 INCORRECT**：剩器件记账差（MN +306 / MP +354）、网数差、VDD 连接差（+935）。

---

## 2. 工作区整理

### 2.1 判定语义（沿用本项目第九轮定的规矩，不另发明）

| 类别 | 判定 | 处置 |
|---|---|---|
| 工作记录 / 证据 / 被取代但不可再生的交付件 | 有留存价值 | **归档（移动，可一键回退）** |
| 可从 PDK 或从本轮 run 重新生成的大文件 | 可再生 | **删除（记 path+bytes+md5+理由）** |
| 交付件 / 源码 / 当前报告 / 已封存审计包 | 在用 | **保留不动** |

### 2.2 主机项目 `2025_16bit5Msar`

归档到 **`05_归档\SAR16后端_20260918\`**（沿用项目已有的 `05_归档` 约定，不新建体系）：

| 分类 | 文件 | 字节 |
|---|---|---|
| `01_被取代的交付包`（delivery / v41 / v42 / round3） | 65 | 61 990 075 |
| `02_探针与一次性脚本`（probes 59 + remote_v42 18 + 顶层探针） | 77 | 222 115 |
| `03_被取代的源码与图像`（gds_images ×2 + tb_sar16_v4.sv） | 24 | 3 132 767 |
| `04_历史工作报告`（16 份被取代的 MD/HTML） | 16 | 2 265 991 |
| `05_远端原始抓取` | 21 | 4 181 130 |
| **合计** | **203** | **71 792 078** |

删除：`backend/analysis/raw/remote/lib_{slow,typical}.lib` —— **PDK 标准单元库副本，各 11 MB**，
属厂商数据、不该留在工作区、可从 `$K` 重新取。

**项目：246 MB → 224.6 MB。**

### 2.3 ★ 引用完整性（这一步不做就等于把文档改坏）

对整理后保留的 **604 份文档**做全量扫描，命中 **32 处**指向归档区的引用：

- **3 处活引用已修复**：`tb/run_tb.sh` 的 TB 路径改指归档区；`README.md` 与
  `make_gds_report.py` 文末加归档告示。改前原件备份在 `_audit/handover_tidy_backup_*`。
- **2 类有意未改**（记在归档区 `REFERENCE_UPDATE.md`）：
  1. **`.workbuddy/memory/*.md`** —— 被接手方的记忆，记录的是「当时看到了什么」，
     **改它等于篡改记录**；
  2. **`_audit/TASK-000R_20260918/*`** —— 带 SHA-256 封印的审计包，描述的是
     **整理之前**的树，**改它封印即失效**。

> **所以这两类里的路径在整理后指向不存在的位置，是预期而非漏改。**

归档区带 `README_ARCHIVE_INDEX.md`、`MOVE_LOG.csv`、`restore_archive.py`（干跑已验证列出全部 25 项）、
`cleanup_manifest.json`、`reference_check{,_precise}.json`。

### 2.4 我自己发现并修掉的一个数字错误

第一版 manifest 里 `05_远端原始抓取` 记的是 **27 MB**，而归档区实际只有 **4.2 MB** ——
因为那两个 `.lib` 是**先量后删**。**写进报告的只能是树的现状，不是计划。**
改用 `recalc_manifest.py` 从归档区**实测重算**后才生成报告。

### 2.5 我自己的中间文件

`E:\ReedLab\tmp\sar16_handover\` 从 **23 MB / 295 文件** 收到 **7.5 MB**，分四类：
`tools/`（可复用解析器与工具）、`docs/`（交接文档与清单）、`probes/`（探针脚本与原始输出）、
`evidence/`（拉回的工件）。删掉了 4 份与 VM/交付包重复的 GDS 副本（15.8 MB）。
路径变更后**重跑了报告生成器**验证没有静默失效。

### 2.6 VM 的 `/tmp`

**221 MB / 815 项 → 692 KB / 10 项**（释放 216 MB）。规则：

- 只删 **属主为 `<user>`** 的条目；跳过 `.X11-unix` / `.ICE-unix` / `systemd-private-*` 等系统项；
- `/tmp/run_*.sh` 可删 —— 已核对 `reproduce_all.sh:43-63` **每阶段都从 `$PC/scripts/` 重新同步**；
- 写了 `/tmp/tmp_cleanup_manifest.txt`（名字 + 字节 + mtime）备审计；
- **项目与回退备份一个没动**：`$PC` 仍 78 MB，`rtl/.bak_v50`、`constraints/.bak_v50`、
  `tb/.bak_v50`、`scripts/.bak_pinfix`、`scripts/.bak_globals` 全在。

---

## 3. 总量

| 位置 | 整理前 | 整理后 |
|---|---|---|
| 主机项目 | 246 MB / 1 582 文件 | **224.6 MB / 1 589 文件**（含新增索引与报告） |
| 我的 scratch | 23 MB / 295 文件 | **7.5 MB / 295 文件** |
| VM `/tmp` | 221 MB / 815 项 | **0.7 MB / 10 项** |
| VM 项目 | 79 MB | **78 MB**（仅删探针日志） |

---

## 4. 下一轮（目标未变）

1. **器件记账归因**：MN +306 / MP +354、网数差、VDD +935 连接 —— 逐类落到具体单元/器件。
2. 端口尾巴：178 vs 179 的那 1 个。
3. `hold` 闭合（本轮已定位到"输入端口 + 门控锁存器"，属约束建模）。
4. **F3 块自身 abstract**（`MACRO sar_digi_paper_core == 1`、`PIN == 232`）。
5. 交付 README 的 LEF 计数改正（实测 **112 MACRO / 652 PIN**，原文 115/665）。

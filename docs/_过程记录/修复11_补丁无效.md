# 修复 11：**补丁无效**——`HFSNET` 1841 → 1841，复位锁死未修好

**日期**：2026-09-19 ｜ **状态**：❌ **修复尝试失败，如实记录** ｜ 承接：修复 02–05、修复 10

---

## 1. 跑完了，且跑得很干净

```
PNR_STATUS=COMPLETE
PNR_RC=0
Elapsed time for this session: 244 seconds
```

产物齐全（GDS / LEF / SDC / SPEF / 网表全部写出，`sar_digi_paper_core.lef 179 611 B`）。

## 2. 判据 1 **失败**（修复前 = 修复后）

```
--- criterion 1 -- HFSNET in the new netlist (target 0, was 1841) ---
  HFSNET lines : 1841              <-- 目标 0，完全没变
  rst_n conns  : 2
  6160:    .clk ( ctosc_gls_0 ) , .rst_n ( HFSNET_119 ) ,
  6213:    .rst_n ( HFSNET_119 ) , .start ( srm_start ) ,
```

**⇒ 打补丁重跑之后，常量网络与复位连接与修复前逐字相同。**
**⇒ 复位锁死**没有**被修好。**

## 3. 补丁的两半分别是什么结果

| 补丁 | 执行结果 | 对判据 1 的作用 |
|---|---|---|
| `set_app_options -name compile.flow.tie_unused_hier_inputs_to_constants -value false` | **`FIX_OK`（被接受）** | **无效**——常量照样产生 |
| `connect_tie_cells -tie_high_lib_cell … -tie_low_lib_cell …` | **`FIX_NOTE`（失败，消息为空）** | 未生效 |

**⇒ 两半都没解决问题。** 我原先的推断链里有一环是错的：
**"关掉 tie-unused-hier-inputs 就能让常量不再产生"——实测被否。**

## 4. 因此现在**能说**与**不能说**的

| 能说 | 不能说 |
|---|---|
| 我按修复 02–05 的推断做了补丁，**跑通、跑完、原工程未损**，但**判据 1 未通过** | 「复位问题已修复」——**没有** |
| 关掉那个选项**不足以**阻止常量产生 | 「关掉它是必要的」——也**不知道** |
| `connect_tie_cells` 的**参数形式不对**（空错误消息） | 「库里没有 tie cell」——修复 02 已证明**有** |

## 5. 下一轮的方向（顺序明确）

1. **先拿 `connect_tie_cells` 的正确用法**：`help connect_tie_cells` 或 `connect_tie_cells -help`，
   把**真实**的选项名与必填参数打出来（我这次是凭猜测写的，错了）；
2. **再查常量的真正来源**：网表里常量根是 `HFSNET_120…124`，它们**在 DC 网表里不存在**
   （DC 网表 `HFSNET` 0 行），所以是 **FC 造出来的**。既然关选项无效，
   要看 FC 日志里**常量是在哪一步引入的**（在 `fc_pnr.log` 里搜 `constant`/`tie` 的**上下文**，
   而不是只搜关键词）；
3. **重新做判据**：新网表 `HFSNET` = 0 且 `.rst_n(rst_n)`。

## 6. 安全情况

- **原工程完好**：`ORIGINAL pnr/out INTACT`，原网表 `HFSNET` 仍 **1841**；
- 所有写入都在副本 `/home/<user>/sar16_work/proj_paper_core_fixtie`；
- 硬门禁全程有效（GATE OK 之后才跑，且跑的是副本路径）。
